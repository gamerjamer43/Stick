// immutable bindings should throw an error when reassigned
let imm = 1
imm = 2

// mutable bindings... yeah
let mutable m: i8 = 1
m = 2

// immutable bindings shouldn't mutate on unary ops either
let imm_counter: i8 = 1
imm_counter++

// mutable bindings should be... well.. mutable
let mutable counter: i8 = 1
counter++

// first reassignment should work
let mutable a: i8 = 1
a = 2

// block should resolve properly due to constant folding
a = {
    let inner = 3
    inner
}

// should overflow the i8
a = 128

// should fail to assign (type mismatch)
let mutable b: bool = false
b = 1

// first add should work
let mutable c: i8 = 1
c += 2

// second should overload
c += 200
